@model Form112.Models.SearchViewModel

@{
    ViewBag.Title = "Result";
}

@section SearchForm {
    @Html.Action("Index", "Search", new { ssvm = Model.XmlSearchViewModel });
}
<table>

    <thead>
        <tr>
            <th>Destination</th>
            <th>Port de départ</th>
            <th>Date de départ</th>
            <th>Durée</th>
            <th>Prix</th>
            <th>Thème</th>
            <th>Description</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Result)
        {
            <tr>
                <td>@item.Ports.Pays.Nom</td>
                <td>@item.Ports.Nom</td>
                <td>@item.DateDepart.Day/@item.DateDepart.Month/@item.DateDepart.Year</td>
                <td>@item.Durees.NbJours</td>
                <td>@item.Prix</td>
                <td>@item.Themes.Libelle</td>
                <td>@item.Description</td>
            </tr>
        }
</tbody>



</table>
